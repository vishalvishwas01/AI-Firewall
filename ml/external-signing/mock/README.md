# MOCK_ONLY external-signing fixture

This directory is a structural test fixture only. Its signatures and public keys are intentionally fabricated and are not cryptographically valid, trusted, deployable, or release-authorized.

Use it only to exercise package-shape, digest, compatibility, replay, and rollback error handling. Never copy these values into a release system.
