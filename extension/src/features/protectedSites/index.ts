/** Protected-site feature boundary. */
export {
  defaultProtectedSites,
  getProtectedSites,
  saveProtectedSites
} from "../storage/storage"
export type { ProtectedSite } from "../../firewall/types"
