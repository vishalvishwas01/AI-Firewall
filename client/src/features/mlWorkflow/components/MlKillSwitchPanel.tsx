import { useEffect, useState } from "react"
import { getMlControl, setMlControl } from "../api"
export function MlKillSwitchPanel() { const [enabled, setEnabled] = useState(false); const [busy, setBusy] = useState(true); const [error, setError] = useState(""); useEffect(() => { void getMlControl().then((value) => setEnabled(value.killSwitchEnabled)).catch(() => setError("Unable to load kill-switch state.")).finally(() => setBusy(false)) }, []); const toggle = async () => { setBusy(true); setError(""); try { setEnabled((await setMlControl(!enabled)).killSwitchEnabled) } catch { setError("Unable to update kill-switch state.") } finally { setBusy(false) } }; return <div className="mx-5 mt-6 rounded-2xl border border-amber-200 bg-amber-50 p-4 sm:mx-8 lg:mx-12"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="font-semibold text-amber-900">AI/ML kill switch</p><p className="text-xs text-amber-800">Disables new ML work and AI calls. Local extension protection remains active.</p></div><button type="button" role="switch" aria-checked={enabled} disabled={busy} onClick={() => void toggle()} className={`rounded-full px-4 py-2 text-sm font-semibold ${enabled ? "bg-rose-700 text-white" : "bg-slate-200 text-slate-800"}`}>{busy ? "Updating…" : enabled ? "ON · Kill ML" : "OFF · ML enabled"}</button></div>{error ? <p role="alert" className="mt-2 text-xs text-rose-700">{error}</p> : null}</div> }
